{
  "name": "study-room",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/main.js && node --check src/data/repository.js && node --check src/lib/supabase.js && node --check src/utils.js"
  }
}
